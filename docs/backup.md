# 配置备份与恢复

系统 → 备份提供本机配置的 JSON 导出与导入，覆盖 API 连接、外观、显示、语言和 AI 工作台设置。

导出默认会移除 API 密钥与 Token。迁移到新设备时，可在可信环境中勾选“包含 API 密钥和 Token”；导入不含密钥的备份会保留目标设备当前已有的密钥。

备份文件使用 `gvfi-config-backup` schema 和版本号校验。导入前会检查 schema、版本和基本结构，格式不匹配的文件不会修改任何设置。
